apiVersion: v1
kind: Secret
metadata:
  name: my-app-secret
type: Opaque
stringData:
  # Simulated database password (sensitive, not in production)
  DB_PASSWORD: "example-password-123"

  # Simulated API key used for authentication
  API_KEY: "demo-api-key-abc-456"

  # Simulated secret file contents
  credentials.txt: |
    token=example-token-xyz
    user=example-user
